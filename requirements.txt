rich>=13.0
numpy>=1.26
pandas>=2.2
lightgbm>=4.3
scikit-learn>=1.4
joblib>=1.3
matplotlib>=3.8
pyarrow>=15.0
# Image recognition (optional — required for --images flag)
ultralytics>=8.0       # YOLOv8 local garment detection        (--vision yolo)
transformers>=4.35     # CLIP clothing analyser                 (--vision clip)
torch>=2.0             # PyTorch backend for CLIP               (--vision clip)
Pillow>=10.0           # PIL image loading (all vision backends)
google-genai           # Gemini AI narrative advice (needs GEMINI_API_KEY env var)
# Packing optimizer (optional — required for --optimize / --optimize-items flags)
pygad>=3.3             # NSGA-II genetic algorithm
ortools>=9.8           # CP-SAT knapsack solver (pure-DP fallback used if absent)
